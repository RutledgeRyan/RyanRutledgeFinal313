function Header(props) {
    return (
        <header className="App-header">
            <h1>{props.headingText}</h1>
            <h2>{props.headingcareer}</h2>
        </header>
    )
}

export default Header